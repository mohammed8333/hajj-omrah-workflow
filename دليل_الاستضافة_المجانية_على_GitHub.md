# دليل الاستضافة والتشغيل المجاني المباشر على GitHub Pages 🕋

هذا الدليل يشرح لك خطوة بخطوة باللغة العربية البسيطة كيف ترفع هذا البرنامج على حسابك في **GitHub** وتشغله عبر رابط إنترنت عام مجاناً مدى الحياة، **بدون الحاجة لأي منصة ثانية** (بدون Oracle Cloud، بدون Railway، بدون Vercel، وبدون أي سيرفرات أو بطاقات بنكية).

---

## 🌟 لماذا هذه النسخة فريدة ومثالية لـ GitHub؟

1. **لا تتطلب سيرفر أو قاعدة بيانات خارجية**: تعمل بنظام قاعدة بيانات محلية ذكية مدمجة في المتصفح (`IndexedDB` و `LocalStorage`) لحفظ المعاملات، المعتمرين، والجوازات بأمان.
2. **استضافة مجانية 100% مدى الحياة**: يتم تشغيلها مباشرة عبر خدمة **GitHub Pages** الرسمية والمجانية.
3. **لا تظهر أي أخطاء 404 عند التحديث**: مهيأة بنظام مسارات نسبي وسلس (`HashRouter`) متوافق تماماً مع أي رابط مستودع على GitHub.
4. **حفظ وتصدير البيانات**: تتيح لك تصدير نسخة احتياطية من جميع معاملاتك بملف `JSON` بضغطة زر ونقلها لأي جهاز آخر.
5. **جاهزة بحسابات تجريبية**: مزودة فورياً بالحسابات الأربعة:
   - **مرسل المعاملات**: `sender` / `Sender@123456`
   - **موظف الصفا**: `safa_emp` / `Safa@123456`
   - **الوكيل السعودي**: `saudi_agent` / `Agent@123456`
   - **مدير النظام**: `admin` / `Admin@123456`

---

## 🚀 طريقة الاستضافة في 3 خطوات سهلة وسريعة

### الخطوة 1: إنشاء مستودع جديد على GitHub (New Repository)
1. ادخل على حسابك في [GitHub.com](https://github.com) وسجل دخولك.
2. اضغط على علامة **+** في أعلى الصفحة واختر **New repository**.
3. اكتب اسماً للمشروع في خانة **Repository name** (مثلاً: `omrah-workflow` أو أي اسم تريده).
4. اجعل المستودع **Public** (عام) لأن ميزة GitHub Pages المجانية تعمل على المستودعات العامة.
5. اضغط على الزر الأخضر في الأسفل: **Create repository**.

---

### الخطوة 2: رفع ملفات المجلد `program2` إلى المستودع

لديك خياران لرفع الملفات:

#### الخيار أ: عبر سطر الأوامر (الأسرع للمطورين)
افتح موجه الأوامر (PowerShell أو Terminal) داخل المجلد `program2` ونفذ الأوامر التالية (استبدل الرابط برابط مستودعك):

```bash
git init
git add .
git commit -m "إطلاق نسخة النظام القابلة للاستضافة على GitHub Pages"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPOSITORY.git
git push -u origin main
```

*(استبدل `YOUR_USERNAME` باسم حسابك و `YOUR_REPOSITORY` باسم المستودع).*

#### الخيار ب: عبر واجهة الموقع (بدون أوامر نهائياً)
1. في صفحة المستودع الجديد على GitHub، اضغط على زر **uploading an existing file**.
2. اسحب جميع الملفات والمجلدات الموجودة داخل مجلد `program2` وأفلتها في المتصفح.
3. اضغط **Commit changes**.

---

### الخطوة 3: تفعيل الاستضافة بضغطة زر (GitHub Pages)

1. من داخل صفحة مستودعك على GitHub، اضغط على تبويب **Settings** (الإعدادات) في الشريط العلوي.
2. من القائمة الجانبية اليسرى، اضغط على **Pages**.
3. في قسم **Build and deployment**:
   * أمام **Source** اختر: **GitHub Actions** (سيتكفل الملف الذكي `.github/workflows/deploy.yml` ببناء الموقع ونشره تلقائياً).
   * **أو** اختر: **Deploy from a branch**، ثم حدد الفرع `main` والمجلد `/docs` واضغط **Save**.
4. خلال دقيقة واحدة، سيظهر لك في أعلى الصفحة رابط موقعك الأخضر الجاهز للعمل:
   ```text
   https://<your-username>.github.io/<your-repository>/
   ```
5. اضغط على الرابط، ومبروك! موقعك الآن مباشر على الإنترنت بالكامل ويمكن لأي شخص فتحه من الكمبيوتر أو الهاتف.

---

## 🔐 حسابات الدخول التجريبية الفورية

عند فتح الرابط، ستجد شاشة الدخول مزودة بأزرار تعبئة تلقائية سريعة للحسابات الأربعة:

| الدور | اسم المستخدم | كلمة المرور | الوظيفة والصلاحيات |
| :--- | :--- | :--- | :--- |
| **مرسل المعاملات** | `sender` | `Sender@123456` | إنشاء الأفواج، إضافة المعتمرين، رفع الجوازات، حل التصحيحات |
| **موظف الصفا** | `safa_emp` | `Safa@123456` | تدقيق المستندات والجوازات، توليد وإدخال رقم نسك، الإحالة للوكيل |
| **الوكيل السعودي** | `saudi_agent` | `Agent@123456` | استلام المعاملات، مراجعتها، إصدار التأشيرات، إتمام المعاملة |
| **مدير النظام** | `admin` | `Admin@123456` | لوحة الإحصائيات الشاملة، إدارة المستخدمين، سجل التدقيق الأمني |

---

## 💾 كيف تحافظ على بياناتك وتأخذ نسخة احتياطية؟

1. في الشريط العلوي للموقع، ستجد زراً بعنوان **"البيانات والنسخ"**.
2. عند الضغط عليه، يمكنك:
   - **تصدير نسخة احتياطية (JSON)**: لتحميل ملف يحتوي على كل الأفواج والمعتمرين وسجلات التدقيق إلى جهازك.
   - **استيراد نسخة احتياطية**: لاستعادة بياناتك على أي متصفح أو هاتف آخر في أي وقت.
   - **استعادة البيانات الافتراضية**: لإعادة ضبط النظام إلى حالته التجريبية الأصلية إن أردت.
